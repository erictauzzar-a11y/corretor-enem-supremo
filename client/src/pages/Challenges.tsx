import { useEffect, useState } from "react";
import { Link } from "wouter";
import { ArrowLeft, Award, Check, Flame } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import AuthDialog from "@/components/AuthDialog";
import { supabase } from "@/lib/supabase";
const days = ["Introdução", "Tese", "Argumentação", "Repertório", "Coesão", "Conclusão", "Redação completa"];
export default function Challenges() { const [authUser, setAuthUser] = useState<import("@supabase/supabase-js").User | null>(null); const [done, setDone] = useState<boolean[]>(() => JSON.parse(localStorage.getItem("aprovai-challenge") || "[]")); useEffect(() => { if (!supabase) return; void supabase.auth.getUser().then(({ data }) => setAuthUser(data.user)); }, []); if (!authUser) return <main className="flex min-h-screen items-center justify-center bg-[#f7f8fc] px-5"><Card className="max-w-md rounded-3xl text-center"><CardContent className="p-8"><Flame className="mx-auto h-10 w-10 text-[#3155d8]" /><h1 className="mt-4 text-2xl font-extrabold">Desafio de 7 dias</h1><p className="mt-3 text-sm leading-6 text-[#66708a]">Entre para acompanhar seu progresso de treino.</p><AuthDialog user={authUser} onUserChange={setAuthUser} triggerLabel="Entrar ou criar conta" triggerClassName="mt-6 h-11 rounded-xl bg-[#3155d8] text-white hover:bg-[#2546c3] hover:text-white" /></CardContent></Card></main>; const completed = done.filter(Boolean).length; const toggle = (index: number) => { const next = [...done]; next[index] = !next[index]; setDone(next); localStorage.setItem("aprovai-challenge", JSON.stringify(next)); }; return <main className="min-h-screen bg-[#f7f8fc] px-5 py-8 text-[#17233d] sm:py-12"><div className="mx-auto max-w-4xl"><header className="flex flex-wrap items-center justify-between gap-4"><div><Link href="/painel"><span className="flex items-center gap-2 text-sm font-semibold text-[#66708a] hover:text-[#3155d8]"><ArrowLeft className="h-4 w-4" />Voltar ao painel</span></Link><h1 className="mt-5 text-3xl font-extrabold">Desafio de 7 dias</h1><p className="mt-2 text-sm text-[#66708a]">Uma rotina leve para manter constância sem pressão.</p></div><AuthDialog user={authUser} onUserChange={setAuthUser} /></header><Card className="mt-8 border-[#dfe5ff] bg-[#f7f9ff]"><CardContent className="flex flex-wrap items-center justify-between gap-4 p-6"><div><p className="text-xs font-bold uppercase tracking-wider text-[#3155d8]">Seu progresso</p><p className="mt-2 text-3xl font-extrabold">{completed}/7</p></div><div className="h-3 min-w-[220px] flex-1 overflow-hidden rounded-full bg-white"><div className="h-full rounded-full bg-[#3155d8] transition-all" style={{ width: `${completed / 7 * 100}%` }} /></div>{completed === 7 && <div className="flex items-center gap-2 text-sm font-bold text-[#24734a]"><Award className="h-5 w-5" />Desafio concluído</div>}</CardContent></Card><Card className="mt-5 border-[#e7eaf2] shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><Flame className="h-5 w-5 text-[#3155d8]" />Treino diário</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2">{days.map((day, index) => <button key={day} onClick={() => toggle(index)} className={`flex items-center gap-3 rounded-2xl p-4 text-left transition ${done[index] ? "bg-[#f1faf4] text-[#24734a]" : "bg-[#f7f8fc] text-[#59657f] hover:bg-[#eef2ff]"}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full text-xs font-bold ${done[index] ? "bg-[#d6f1df]" : "bg-white ring-1 ring-[#e1e5ef]"}`}>{done[index] ? <Check className="h-4 w-4" /> : index + 1}</span><span><strong className="block text-sm">Dia {index + 1} — {day}</strong><small className="mt-1 block text-xs opacity-75">Marque quando concluir seu treino.</small></span></button>)}</CardContent></Card><Link href="/treinos"><Button variant="outline" className="mt-5 rounded-xl border-[#dfe4ef]">Ir para treinamentos</Button></Link></div></main>; }
